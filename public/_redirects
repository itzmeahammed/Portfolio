# Netlify redirects file for React Router SPA
# This ensures all routes are handled by index.html for client-side routing

# Handle all routes by serving index.html
/*    /index.html   200

# Optional: Redirect common 404 patterns to home
/404  /   302

# Optional: Handle API routes if you add them later
/api/*  /api/:splat  200

# Optional: Force HTTPS (uncomment if needed)
# http://yoursite.netlify.app/*  https://yoursite.netlify.app/:splat  301!
# http://www.yoursite.netlify.app/*  https://yoursite.netlify.app/:splat  301!
